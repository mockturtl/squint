name: squint
version: 0.1.0
description: A minimal command-line application.
environment:
  sdk: '>=1.0.0 <2.0.0'
dependencies:
  log_init:
    path: ../log_init
  logging: any
  #async_await:
    #git: https://github.com/dart-lang/async_await.git
dev_dependencies:
  unittest: any
  mock: any
executables:
  squint: main
transformers:
#- async_await
- $dart2js:
    $exclude: 'test/**'
