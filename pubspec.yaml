name: squint
version: 0.1.0
description: Issue labels for hackers.
author: mockturtl
#homepage: https://mockturtl.github.io/squint
environment:
  sdk: '>=1.8.0 <2.0.0'
dependencies:
  pico_log:
    path: ../pico_log
  dotenv:
    path: ../dotenv
  logging: '^0.9.0'
  #async_await:
    #git: https://github.com/dart-lang/async_await.git
dev_dependencies:
  unittest: any
  mock: any
executables:
  squint: main
transformers:
#- async_await
- $dart2js:
    $exclude: 'test/**'
